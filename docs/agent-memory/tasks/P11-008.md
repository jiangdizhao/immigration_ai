# P11-008 — Appointment / Consultation Workflow

**Status:** ACTIVE — STAGES 1–3 SOURCE ACCEPTED / RUNTIME GATES A–C ACCEPTED / GATE D NEXT  
**Milestone:** Phase 11  
**Current source checkpoint:** `62947e779b8a5a39df58038deab7c135e452569f`  
**Original planning checkpoint:** `c507467444f4b5304150f71c9e8aa2354cdfd6e9`  
**Task granularity:** one major task packet with three internal stages.

## Goal

Replace appointment placeholders with a durable, secure, Chinese-first bilingual consultation workflow that can actually move a customer from a request for a meeting to a staff-proposed slot and an explicitly confirmed consultation.

P11-008 is a scheduling/service workflow. It is not a legal-reasoning feature, not a calendar-provider integration, not a payment/pricing feature, and not a matter-wide staff browser.

## Repository baseline

At the planning checkpoint:

- P11-007 Lawyer Workspace Continuity is VERIFIED.
- AI Workspace `handleBookConsultation` still shows a bounded toast instead of creating an appointment.
- Contact routes users into the existing website workflow but does not persist a consultation request.
- No appointment/consultation table, state machine, customer history, staff scheduling queue, or external calendar adapter exists.
- Existing `LawyerClarificationRequest` is an immutable answer-review/handoff workflow and must not be repurposed into a scheduling table.
- Existing email infrastructure may be reused later through a consultation-specific notification adapter.
- D-040 and D-043 remain in force.

## Product boundary

P11-008 implements a first-party request/proposal/confirmation workflow.

Do **not** implement a fake calendar showing invented free slots.

Do **not** assume Google Calendar, Microsoft 365, Calendly, Zoom, Teams, phone, office address, or any other provider/coordinate.

Do **not** invent consultation price, VIP eligibility, office hours, SLA, lawyer specialisation, or meeting duration.

## Actors

### Customer

Must be:

- authenticated;
- non-guest;
- email verified;
- role `user`.

Customer may:

- create consultation request;
- list/read own requests;
- confirm a staff proposal;
- request rescheduling;
- cancel a non-terminal request.

### Admin

May:

- list/read all consultation requests;
- assign/unassign a verified lawyer;
- propose/re-propose a slot;
- cancel;
- mark a confirmed consultation completed.

### Lawyer

May:

- list/read only consultations assigned to their user ID;
- propose/re-propose a slot for an assigned request;
- cancel an assigned request;
- mark an assigned confirmed consultation completed.

A lawyer must not self-assign arbitrary consultation requests.

## Continuity / least privilege

A consultation may optionally link to:

- an owned `Chat` / `ImmigrationConversation`;
- an owned `LawyerClarificationRequest`.

The server must validate ownership before persisting either link.

If a chat is linked, derive `legalMatterId` from the owned server record; never trust a client-supplied matter ID.

These links do not widen staff authorization:

- appointment assignment does not grant full chat history;
- appointment assignment does not grant MatterDocument access;
- appointment assignment does not grant unrelated lawyer-request access;
- appointment assignment does not grant arbitrary Legal Service Matter access.

Normal staff appointment projections should contain only scheduling-safe data: customer identity, customer note, preferred windows/timezone/method, appointment state, assigned lawyer identity, and staff-proposed slot/method/instructions.

## Proposed schema

Use additive schema. Exact naming may follow repository conventions, but preserve the contract below.

### ConsultationRequest

Required concepts:

- `id` UUID primary key;
- `userId` owner, FK -> User, RESTRICT;
- optional `chatId`, FK -> Chat, SET NULL;
- optional server-derived `legalMatterId`;
- optional `lawyerClarificationRequestId`, FK -> LawyerClarificationRequest, SET NULL;
- `status`: `requested | proposed | confirmed | completed | cancelled`;
- `customerTimezone`: bounded IANA timezone string;
- `preferredWindows`: JSON array, max 3 validated future windows stored as normalized UTC instants;
- `methodPreference`: `video | phone | in_person | no_preference`;
- bounded `customerNote`;
- optional `assignedLawyerUserId`, FK -> User, SET NULL;
- optional `assignedAt`;
- optional scheduled `startAt` / `endAt`;
- optional `scheduledMethod`: `video | phone | in_person | other`;
- optional bounded `meetingInstructions`;
- optional `proposedAt`, `confirmedAt`, `completedAt`, `cancelledAt`;
- `createdAt`, `updatedAt`.

Indexes should support:

- customer history by owner/status/update time;
- admin queue by status/update time;
- assigned-lawyer queue by lawyer/status/update time;
- overlap query by lawyer + scheduled interval/status.

### ConsultationEvent

Immutable audit/event rows:

- `id`;
- `consultationRequestId`;
- optional actor user ID;
- actor role `customer | lawyer | admin | system`;
- bounded event type;
- optional from/to status;
- bounded metadata containing scheduling transition facts only;
- created timestamp.

Do not store secrets, auth tokens, raw chat content, document text, or provider payloads in event metadata.

## Time contract

All server persistence is UTC-consistent with the existing chatbot DB session policy.

Customer timezone must be a valid IANA timezone accepted by the server runtime.

Preferred windows:

- maximum 3;
- each has `startAt < endAt`;
- each must be in the future when submitted;
- reject malformed/duplicate/overlapping windows from the same request;
- use bounded horizon/length validation chosen conservatively and documented in tests.

Staff proposal:

- requires assigned lawyer;
- requires valid concrete `startAt < endAt`;
- requires a scheduled method;
- meeting instructions are optional bounded plain text;
- proposed/confirmed appointments for the same lawyer must not overlap.

## Concurrency / overlap invariant

Do not rely only on client checks.

When proposing/re-proposing a slot:

1. acquire a transaction-scoped per-lawyer advisory lock or an equivalently strong repository-compatible serialization mechanism;
2. re-read the current request/state;
3. query other `proposed` or `confirmed` consultations for that lawyer whose half-open interval overlaps `[startAt, endAt)`;
4. reject with a domain conflict if overlap exists;
5. update state and append the immutable event in the same transaction.

This protects against two concurrent staff actions booking the same lawyer into overlapping slots.

## State machine

Baseline:

```text
requested -> proposed -> confirmed -> completed
```

Allowed side paths:

- requested -> cancelled;
- proposed -> requested when customer requests rescheduling;
- proposed -> proposed when authorized staff re-proposes;
- proposed -> cancelled;
- confirmed -> proposed when authorized staff must replace the slot; fresh customer confirmation is required;
- confirmed -> cancelled;
- confirmed -> completed;
- completed and cancelled are terminal.

Invalid transitions must fail server-side.

Customer confirmation must use optimistic/current-state validation so a stale proposal cannot be confirmed after it has been replaced.

## Stage 1 — Consultation domain foundation

Implement only the domain foundation.

Expected scope:

- schema additions;
- next generated migration artifact after repository `0021`; do not apply;
- consultation types/status/action helpers;
- access/RBAC helpers;
- pure input validation for timezone/windows/methods;
- service layer with transactions, immutable events, optimistic state checks, assignment validation and overlap protection;
- customer API for create/list/read/confirm/reschedule/cancel;
- admin API for list/read/assign/propose/cancel/complete;
- lawyer API for assigned-only list/read/propose/cancel/complete;
- deterministic tests.

No visible UI work in Stage 1 except the minimum needed for API/test integration.

No email sending in Stage 1.

No external provider call.

### Stage 1 API rules

Customer create must not accept:

- `assignedLawyerUserId`;
- `legalMatterId`;
- staff slot fields;
- staff meeting instructions.

If `chatId` or `lawyerClarificationRequestId` is supplied, re-authorize ownership and derive safe linkage server-side.

Admin assignment may target only verified, non-guest role=lawyer accounts.

Lawyer routes must enforce `assignedLawyerUserId === session.user.id`.

Return allowlisted DTOs; do not spread raw DB rows when that would expose fields not intended for the actor.

### Stage 1 tests

At minimum:

#### Customer access

- unauthenticated rejected;
- guest rejected;
- lawyer/admin cannot use customer-create route as ordinary customer;
- customer can create/list/read own request;
- customer cannot read another customer's request;
- linked chat ownership is enforced;
- linked lawyer request ownership is enforced;
- client-supplied matter/assignment escalation is impossible.

#### Validation

- invalid timezone rejected;
- zero windows rejected if windows are required by chosen contract;
- >3 windows rejected;
- malformed/reversed/past windows rejected;
- duplicate/overlapping customer windows rejected;
- oversized note/instructions rejected;
- invalid method rejected.

#### Assignment / staff RBAC

- admin can assign verified lawyer;
- admin cannot assign user/unverified/guest;
- lawyer sees only assigned consultations;
- another lawyer receives 403/no disclosure;
- lawyer cannot self-assign.

#### State machine

- requested -> proposed;
- proposed -> confirmed;
- proposed -> requested reschedule;
- confirmed -> proposed re-proposal;
- confirmed -> completed;
- cancellation from allowed states;
- terminal-state mutation rejected;
- stale confirmation rejected after re-proposal.

#### Overlap

- same lawyer overlapping proposed/confirmed slot rejected;
- adjacent half-open intervals allowed;
- different lawyers may have overlapping clock times;
- cancelled/completed historical requests do not block a new slot;
- concurrency/serialization behavior is covered at the service boundary as far as the repository test environment permits.

#### Audit

- creation, assignment, proposal, confirmation, reschedule, cancellation and completion write immutable events;
- failed transaction does not leave partial state/event mutations.

## Stage 2 — Customer booking continuity

After Stage 1 source review/acceptance:

- add `/consultations` history;
- add `/consultations/new`;
- add `/consultations/[id]`;
- Chinese-first bilingual UI using existing locale;
- AI Workspace booking CTA opens the real consultation flow with optional owned chat context;
- Contact page CTA opens the real consultation flow rather than only AI intake;
- Client Portal surfaces safe consultation summary/history;
- customer confirms, requests reschedule, or cancels;
- desktop/mobile responsive acceptance.

Do not expose staff-only scheduling controls.

## Stage 3 — Staff scheduling + notifications + E2E

After Stage 2 acceptance:

- admin consultation queue/detail and assignment;
- assigned-lawyer consultation queue/detail;
- propose/re-propose slot and method/instructions;
- complete/cancel actions;
- consultation-specific email notification adapter using existing email infrastructure;
- notification delivery is fail-neutral to the committed DB transition;
- no provider/calendar integration is required;
- full local E2E with separate customer/admin/lawyer identities;
- desktop/mobile zh-CN/English visual acceptance.

## Notification boundary

Do not overload `LawyerRequestNotification` types.

Create a consultation-specific notification contract.

Notification failure must not roll back a valid appointment state transition.

Do not include private chat/document content in email.

## Commercial/provider boundary

P11-008 does not decide:

- consultation fee;
- VIP requirement;
- payment requirement;
- service duration;
- office hours;
- lawyer availability calendar;
- video provider;
- phone number;
- office location.

If product implementation requires any of these values, STOP — DECISION REQUIRED.

## No Legal Service changes expected

The appointment workflow is a chatbot service domain.

Do not edit `legal-service/` for P11-008 unless a concrete requirement proves it unavoidable. If that occurs, stop for architecture review first.

## Migration discipline

Stage 1 may generate the next migration artifact.

Do not apply it locally, staging, or production without explicit authorization.

Do not rewrite `0018`–`0021`.

P11-005 remains NOT VERIFIED under D-040.

## Validation / workflow

Follow the project gated workflow:

1. coding model implements Stage 1 locally only;
2. no commit / no push;
3. owner runs and reports `git status --short` and `git diff --stat`;
4. ChatGPT reviews the bounded diff;
5. owner commits/pushes only with exact approved commands;
6. ChatGPT directly reviews remote GitHub source;
7. only then proceed to Stage 2.

Use repository-declared frontend package manager and existing test conventions.

Do not deploy, apply migration, merge, rebase, or force-push.


## Stage 1 source acceptance — 2026-09-26

Stage 1 source gate is **ACCEPTED** at `83506156546dcff2944b21edef16423a5b402d54`.

Accepted implementation:

- `ConsultationRequest` / `ConsultationEvent`;
- migration `0022_first_slayback.sql` tracked but unapplied;
- revision-based optimistic concurrency;
- immutable transactional events;
- customer/admin/assigned-lawyer API/RBAC;
- verified-lawyer admin assignment;
- target-User row serialization between assignment and lawyer demotion;
- transaction-scoped per-lawyer advisory lock + half-open interval overlap protection;
- customer/staff projection separation;
- exact continuity ownership and cross-link consistency;
- rollout-safe lawyer demotion while ConsultationRequest is absent.

Recorded final validation: 326 unit tests passed; production build passed; changed-file Biome passed; `git diff --check` passed; second `pnpm db:generate` reported no schema changes.

Not yet verified: real migrated-DB concurrency, rollback and API execution. These remain under the P11-008 migrated-DB transaction gate.

## Stage 2 detailed execution contract — Customer booking continuity

Stage 2 begins from source checkpoint `83506156546dcff2944b21edef16423a5b402d54`.

### Stage 2 goal

Turn the Stage 1 customer APIs into a coherent Chinese-first bilingual customer journey without adding staff scheduling or inventing availability.

Required customer routes:

- `/consultations` — own consultation history;
- `/consultations/new` — create request;
- `/consultations/[id]` — own consultation detail and status-valid actions.

### Customer form

The create form must collect only Stage 1-authorized fields:

- 1–3 preferred future time windows;
- customer timezone;
- method preference: video / phone / in-person / no preference;
- optional bounded customer note;
- optional owned `chatId` continuity;
- optional owned `lawyerClarificationRequestId` only when a real customer surface explicitly supplies it.

Do not expose or accept trusted customer input for:

- `legalMatterId`;
- assigned lawyer;
- staff proposal fields;
- meeting instructions;
- lifecycle timestamps;
- revision.

### Time-input implementation

Initial Stage 2 UI uses the browser/system timezone only:

1. obtain `Intl.DateTimeFormat().resolvedOptions().timeZone`;
2. validate that a non-empty IANA timezone was resolved;
3. display the timezone near the preferred-window controls;
4. use native `datetime-local` inputs;
5. convert each local value with `new Date(value).toISOString()`, which is valid only because the stored customer timezone is the same browser/system timezone;
6. do not allow an arbitrary different timezone label without a correct timezone-aware conversion implementation.

Display stored preferred/proposed times with `Intl.DateTimeFormat(..., { timeZone: consultation.customerTimezone })`.

### History/detail behavior

History must show safe customer fields only:

- status;
- updated/created time;
- preferred windows;
- assigned yes/no;
- proposed/confirmed slot when present.

Detail must show:

- preferred windows and timezone;
- method preference;
- optional customer note;
- staff proposal, scheduled method and meeting instructions when present;
- lifecycle/status information;
- no lawyer login identity.

Customer actions:

- `requested`: cancel;
- `proposed`: confirm / request reschedule / cancel;
- `confirmed`: cancel;
- `completed|cancelled`: no mutation controls.

All mutations send the current `expectedRevision`. On 409 stale conflict, reload the consultation and show a localized “updated elsewhere / please review” message instead of retrying automatically.

### Entry-point integration

**AI Workspace**

Replace the placeholder booking toast with navigation:

- with active conversation: `/consultations/new?chatId=<owned-chat-id>`;
- without active conversation: `/consultations/new`.

Do not pass `matterId` from the browser.

**Contact**

Make the existing consultation CTA navigate to `/consultations/new`. Preserve public-content governance and do not add unverified contact details, availability, fees or service promises.

**Client Portal**

Add a safe consultation section:

- explicit `available | unavailable` state;
- safe customer consultation summaries when available;
- no false zero when the schema is absent;
- link to `/consultations` for full history;
- selected exact matter group may offer `/consultations/new?chatId=<defaultContinuationChatId>`.

Do not group or infer consultation/matter relationships by titles, text similarity or AI semantics.

**Account menu**

For authenticated customer accounts, add a bilingual consultation-history link if it fits the existing account-menu hierarchy. Do not add it to admin/lawyer account menus.

### Consultation-schema rollout compatibility

Stage 2 must introduce a shared server-only schema-availability boundary for all consultation route families that can be invoked before 0022 is applied.

Preferred mechanism: exact `to_regclass('public."ConsultationRequest"')` catalog check.

When unavailable:

- customer list/create/detail/action APIs return a bounded explicit unavailable response/code;
- admin/lawyer consultation APIs should also fail explicitly rather than producing relation-not-found 500s;
- Client Portal marks consultations unavailable;
- existing non-consultation portal data remains usable.

Do not broad-catch arbitrary database errors.

Do not present unavailable as an empty consultation list.

### Authentication

Reuse existing customer role/auth conventions:

- registered;
- non-guest;
- email verified;
- role=user.

Use existing page/API redirect and no-disclosure patterns. Do not create a separate auth system.

### Bilingual / responsive contract

All Stage 2 static UI copy follows persisted `SiteLocale`:

- Chinese default;
- English switch reactive without requiring a full reload where existing client surfaces already support it.

Do not use assistant answer language to choose consultation-page locale.

Desktop and mobile must preserve:

- readable time windows;
- non-overflowing status/action controls;
- long notes/instructions wrapping;
- accessible labels and focus states.

### Stage 2 tests

At minimum add deterministic coverage for:

- consultation copy/status/action labels in zh-CN/en;
- customer action visibility by status;
- browser-timezone/ISO conversion helper behavior;
- unavailable-vs-empty consultation state;
- Client Portal consultation availability projection;
- exact chatId link generation/encoding;
- no lawyer email/identity in customer UI DTO usage;
- stale 409 refresh behavior at a testable helper/controller boundary if practical;
- consultation schema availability helper semantics.

Run existing consultation/domain tests unchanged.

### Stage 2 source acceptance

Stage 2 source review can pass without applying 0022 if:

- build/unit/changed-file checks pass;
- all existing non-consultation workflows remain rollout-compatible;
- unavailable-schema UI is coherent and does not claim zero history;
- no schema/migration change is introduced;
- no Stage 3 work is included.

Real create/confirm/reschedule/cancel DB E2E remains deferred under the P11-008 migrated-DB gate.

### Stage 2 non-goals

Do not implement:

- admin consultation queue/UI;
- lawyer consultation queue/UI;
- staff assignment/proposal UI;
- consultation email notifications;
- external calendar availability;
- Calendly/Google/Microsoft/Zoom/Teams integrations;
- pricing/payment/VIP-only booking;
- office hours/phone/address;
- Legal Service changes;
- MatterDocument authorization changes;
- migration application or a new migration.

### Stage 2 gated workflow

1. coding model implements Stage 2 locally, uncommitted/unpushed;
2. owner shows `git status --short` + complete `git diff --stat`;
3. external ChatGPT reviews the full patch;
4. only after acceptance: commit/push;
5. ChatGPT directly verifies the remote GitHub diff;
6. visual acceptance covers at least the unavailable-schema state now; real DB-backed customer workflow is deferred until the migrated-DB gate is authorized.


## Stage 2 source acceptance — 2026-09-26

Stage 2 source gate is **ACCEPTED** at `2d91c17a483c0fd30a434536f87b64bc7cf6fe94`.

The accepted customer continuity layer includes:

- verified-customer `/consultations`, `/consultations/new`, `/consultations/[id]`;
- explicit consultation schema availability before 0022 is applied;
- customer history/detail/create unavailable states distinct from real empty data;
- Client Portal `available | schema_unavailable | verification_required`;
- exact chatId-only entry from AI Workspace / Client Portal;
- Contact and customer account navigation;
- browser-IANA preferred-window conversion;
- status-valid customer actions with revision OCC;
- 409 refetch/no automatic retry;
- no lawyer login identity in customer views.

Final recorded validation: 341 unit tests passed; build/Biome/diff-check passed; db generation reported no schema changes.

Migration 0022 remains unapplied and the migrated-DB transaction gate remains open.

## Stage 3 detailed execution contract — Staff scheduling + consultation notifications

Stage 3 begins from source checkpoint `2d91c17a483c0fd30a434536f87b64bc7cf6fe94`.

### Goal

Complete the staff-facing source/UI half of the first-party consultation workflow and add bounded fail-neutral notification infrastructure, while preserving all Stage-1/2 authorization and rollout rules.

Stage 3 does not apply migration 0022 and does not claim real DB E2E acceptance.

### Required staff routes

Add:

- `/admin-portal/consultations`;
- `/admin-portal/consultations/[id]`;
- `/lawyer-portal/consultations`;
- `/lawyer-portal/consultations/[id]`.

Do not change the meaning of:

- `/lawyer-portal` — P11-007 lawyer-review queue remains present;
- `/lawyer-portal/[id]` — P11-007 LawyerClarificationRequest detail.

Add clear navigation between lawyer review work and consultation scheduling rather than merging the ID spaces.

### Staff page access

Admin routes require authenticated non-guest role=admin and should follow the existing admin redirect/access pattern.

Lawyer consultation routes require authenticated non-guest verified role=lawyer and use the existing consultation staff authorization contract.

Do not rely only on session role when the existing consultation API performs a DB role/verification check.

### Queue behavior

Admin queue:

- list all consultation requests returned by `GET /api/admin/consultations`;
- show customer email, status, assignment, proposed/confirmed time when present and updated time;
- link to admin consultation detail;
- schema 503 -> localized unavailable state, not empty queue.

Lawyer queue:

- list only assigned consultations returned by `GET /api/lawyer-portal/consultations`;
- show customer email, status, schedule and updated time;
- link to lawyer consultation detail;
- schema 503 -> localized unavailable state.

Do not add matter/chat/document browsing.

### Admin detail/actions

Use `GET/PATCH /api/admin/consultations/[id]`.

Display scheduling-safe StaffConsultationDTO fields only.

Allowed UI actions must mirror Stage-1:

- `requested`: assign/unassign lawyer, propose if assigned, cancel;
- `proposed`: re-propose, cancel;
- `confirmed`: re-propose requiring fresh customer confirmation, cancel, complete;
- terminal `completed|cancelled`: read-only.

Assignment UI may reuse `GET /api/admin/lawyers` and filter current verified `role=lawyer` accounts. Do not change roles from the consultation detail page.

API/service validation remains authoritative.

### Lawyer detail/actions

Use `GET/PATCH /api/lawyer-portal/consultations/[id]`.

No assignment controls.

Allowed actions:

- assigned `requested`: propose, cancel;
- assigned `proposed`: re-propose, cancel;
- assigned `confirmed`: re-propose, cancel, complete;
- terminal: read-only.

The existing API remains assigned-only authority.

### Proposal form

Collect only:

- concrete start;
- concrete end;
- scheduled method: video / phone / in_person / other;
- optional bounded meeting instructions.

Use native `datetime-local` interpreted in the staff browser/device timezone. Show that IANA timezone visibly. Convert to absolute ISO timestamps.

Do not offer a fake free-slot calendar.

Do not invent duration, office hours, Zoom/Teams/phone/address.

Display customer preferred windows in customerTimezone, clearly labelled as preferences.

Display current proposal in customerTimezone. Optionally also show staff-local time for operator convenience.

### OCC/conflicts

Every PATCH sends current `expectedRevision`.

Never automatically retry a failed mutation.

On 409:

- refetch latest consultation once;
- show localized bounded conflict/review-latest UX;
- for proposal conflicts, tell staff to review the refreshed state and choose another slot if needed;
- do not infer that the database accepted any mutation.

### Rollout compatibility

Migration 0022 remains absent in the current environment.

All new queue/detail pages must therefore:

- call existing authenticated APIs;
- render explicit localized schema-unavailable state on the current DB;
- never show empty queue/history merely because schema is absent;
- leave existing lawyer-review/admin account workflows usable.

Do not broad-catch unrelated DB failures as schema unavailable.

### Consultation notification contract

Create a separate server-only module such as `chatbot/lib/consultations/notifications.ts`.

Do not reuse the `LawyerRequestNotification` type.

Use a consultation-specific feature flag, disabled unless explicitly configured, e.g.:

`CONSULTATION_NOTIFICATIONS_ENABLED=true`

Use the existing SES transport/template infrastructure through a consultation-specific email builder/sender.

Notification delivery must be fail-neutral:

- mutation transaction completes first;
- target lookup/notification occurs after success;
- sender failure is caught/logged safely;
- API still returns the successful DB mutation.

Do not put notification sending inside the mutation transaction.

### Bounded notification events

A sufficient Stage-3 event set is:

- `request_created` -> optional configured staff triage email;
- `request_assigned` -> assigned lawyer;
- `proposal_ready` -> customer;
- `customer_confirmed` -> assigned lawyer when present;
- `reschedule_requested` -> assigned lawyer when present;
- `customer_cancelled` -> assigned lawyer when present, otherwise optional configured staff target;
- `staff_cancelled` -> customer;
- `completed` -> customer.

The implementation may use a slightly smaller equivalent set if it preserves the customer/staff handoff points above.

Do not send notifications for failed/stale mutations.

### Notification target safety

Add a bounded service/helper that returns only:

- customer email;
- assigned lawyer email if present.

An optional staff triage recipient may come only from configuration, such as `CONSULTATION_STAFF_EMAIL`.

Do not expose notification target helpers to the browser.

### Email content

Email must contain only:

- generic consultation event text;
- role-appropriate app link.

Allowed links:

- customer -> `/consultations/{id}`;
- lawyer -> `/lawyer-portal/consultations/{id}`;
- staff/admin -> `/admin-portal/consultations/{id}`.

Do not include:

- customer note;
- preferred-window text;
- chat excerpts;
- document evidence;
- legal facts;
- matter metadata;
- internal traces;
- provider credentials;
- invented meeting links.

No bilingual email requirement is imposed unless the repository already has a verified recipient-locale authority. Generic safe English is acceptable for Stage 3 notification infrastructure.

### Tests

Add deterministic tests for:

- admin/lawyer staff action visibility by status;
- admin assignment controls absent outside requested;
- lawyer has no assignment action;
- staff proposal local-time conversion helper without CI timezone assumptions;
- exact role-specific consultation links;
- schema-unavailable vs empty staff queues;
- 409 staff action -> refetch once / no retry;
- consultation notification types are separate from lawyer-request notifications;
- notification feature flag disabled by default;
- notification sender failure returns fail-neutral false/does not throw from wrapper;
- email template contains only the generic event/link and does not interpolate private note/content.

Retain all Stage-1/2 tests.

### Validation

Run focused Stage-3 tests, full `pnpm test:unit`, build, changed-file Biome, `git diff --check`, and `pnpm db:generate`.

`db:generate` must report no schema changes.

No migration 0023.

No migration application.

### Source stop condition

When Stage 3 source implementation is complete:

- do not commit;
- do not push;
- do not apply 0022;
- do not deploy;
- report exact files, tests, validation, status/stat and notification contract;
- stop for external patch review.

After remote Stage-3 source acceptance, P11-008 moves to the separately authorized migrated-DB/runtime gate before overall verification.


## Stage 3 source acceptance — 2026-09-26

Stage 3 source gate is **ACCEPTED** at `5304742196f08894d249138c30b2245977a52e9b`.

Accepted Stage-3 result:

- admin consultation queue/detail at `/admin-portal/consultations[/id]`;
- assigned-lawyer consultation queue/detail at `/lawyer-portal/consultations[/id]`;
- P11-007 `/lawyer-portal/[id]` remains a separate LawyerClarificationRequest route;
- verified staff page access and existing API/RBAC authority preserved;
- state-valid assignment/proposal/cancel/complete controls;
- explicit staff-browser IANA timezone handling;
- customer-timezone presentation;
- revision OCC and 409 refetch/no automatic retry;
- re-proposal preserves existing method/instructions unless staff changes them;
- explicit unavailable-schema behavior before 0022;
- consultation-specific feature-gated, post-commit, fail-neutral notification layer;
- bounded target lookup and privacy-safe generic email content;
- no schema, migration, Legal Service or provider/calendar integration drift.

Recorded validation: 355 unit tests passed; build/Biome/diff-check passed; db generation reported no schema changes.

## Migrated-DB/runtime acceptance packet

### Purpose

Prove that the accepted P11-008 source actually works with the real PostgreSQL schema and concurrency semantics, without migrating the normal local chatbot database.

### Starting source

`5304742196f08894d249138c30b2245977a52e9b`

### Environment safety

Use a fresh disposable clone of the chatbot database.

Never:

- edit `chatbot/.env.local`;
- apply migrations to the normal local chatbot database;
- use staging/production as the gate DB;
- echo or paste `POSTGRES_URL`;
- contact real SES/AWS/OpenAI/S3/Stripe;
- create migration 0023;
- change schema/migration source merely to make the gate pass.

The disposable URL must be supplied through a shell-scoped `POSTGRES_URL`. The existing migration script loads `.env.local` but normal dotenv semantics do not override an already supplied environment value; nevertheless, verify the target database identity before running migrations.

### Step 1 — preflight

Verify:

- branch is `phase11-chinese-service-platform-ui-rebase`;
- HEAD is `5304742196f08894d249138c30b2245977a52e9b`;
- worktree is clean;
- normal chatbot DB identity and migration journal can be read without printing credentials;
- no disposable DB with the intended name already exists.

Record the normal DB journal before migration.

### Step 2 — disposable clone

Create a uniquely named clone, for example:

`chatbot_p11_008_gate_20260926_<suffix>`

Clone the normal chatbot database using a credential-safe local PostgreSQL method.

Do not terminate source DB sessions or use it as a writable template merely for convenience if that would disturb the running environment.

After cloning, verify the disposable database name using SQL before any migration.

### Step 3 — migrate clone through 0022

From `chatbot/`, run the repository migration runner with the disposable `POSTGRES_URL`.

Expected end state:

- migration journal through `0022_first_slayback`;
- `ConsultationRequest` exists;
- `ConsultationEvent` exists;
- expected consultation indexes/FKs exist;
- no 0023;
- normal chatbot DB journal unchanged.

If migration fails, capture the first error and STOP. Do not hand-edit the clone schema to bypass it.

### Step 4 — DB-backed transaction acceptance

Use deterministic synthetic/test records on the disposable DB.

Required assertions:

1. **Create + event atomicity**
   - create one customer consultation;
   - revision starts at 1;
   - exactly one `created` event exists.

2. **Assignment**
   - admin assigns a verified non-guest lawyer;
   - revision increments;
   - assignment event is present.

3. **Stale OCC**
   - replay a mutation with a stale revision;
   - receive domain conflict;
   - request state and event count do not change.

4. **Same-lawyer overlap race**
   - prepare two requested consultations assigned to the same lawyer;
   - launch overlapping proposals concurrently;
   - at most one overlapping proposal commits;
   - the loser is a conflict;
   - no double-booked proposed/confirmed intervals remain.

5. **Half-open boundary**
   - proposal B starts exactly when proposal A ends;
   - both may commit if otherwise valid.

6. **Confirmation/re-proposal**
   - customer confirms a proposed slot;
   - staff re-proposal moves it back to proposed and clears prior confirmation timestamp;
   - latest method/instructions persist as supplied;
   - customer must confirm again.

7. **Reschedule**
   - customer request-reschedule moves proposed -> requested;
   - active scheduled start/end/method/instructions/proposedAt are cleared.

8. **Cancel/complete**
   - valid non-terminal cancellation succeeds;
   - only confirmed consultation can complete;
   - terminal statuses reject further transitions.

9. **Assigned-lawyer scope**
   - assigned lawyer can read/act;
   - another lawyer receives no consultation access.

10. **Lawyer demotion race**
    - concurrent assignment and demotion must not leave active assigned consultation work on an account whose final role is user.

11. **Request/event rollback atomicity**
    - install a temporary disposable-only fault that forces a selected `ConsultationEvent` insert to fail;
    - invoke the corresponding mutation;
    - verify request row/revision/status changes also rolled back;
    - remove the temporary fault;
    - verify subsequent normal mutation succeeds.

### Step 5 / Gate D — notification fail-neutral runtime smoke

Gate D runs only after Gates A–C are accepted and uses the retained disposable DB.

Keep notifications disabled during fixture setup. Create a fresh synthetic verified customer with an `@example.test` email plus a synthetic admin, then create one consultation through the real consultation service.

For the isolated smoke, reproduce the production post-commit order explicitly:

1. save the process's existing notification/email environment values and console-error function;
2. set `CONSULTATION_NOTIFICATIONS_ENABLED=true`;
3. set `EMAIL_PROVIDER=p11_gate_d_unsupported`;
4. blank `AWS_REGION` / `AWS_DEFAULT_REGION` in the child process as an additional no-network guard;
5. install a bounded console-error capture that records only the emitted structured metadata for later assertions;
6. perform a valid admin `cancelConsultation(...)` mutation on the fresh request;
7. only after that mutation returns successfully, call `notifyConsultation(id, "staff_cancelled")`;
8. require the notification call to return `false` rather than throw;
9. restore console and environment values in `finally`.

The chosen `staff_cancelled` notification targets the synthetic customer from the database and therefore exercises real target lookup, consultation email-template construction, the shared auth-email sender, the deliberately unsupported provider failure, and the consultation delivery wrapper without contacting SES.

Acceptance requires:

- cancellation persisted as `cancelled`;
- revision incremented exactly once;
- exactly one new immutable `cancelled` event persisted;
- notification returned fail-neutral `false`;
- captured metadata proves the auth email path reached purpose `consultation` with provider classified as `unsupported`;
- captured metadata proves `Consultation notification delivery failed` was emitted for kind `staff_cancelled` and recipient `customer`;
- captured logs do **not** contain the synthetic email address, a unique private-note marker, preferred-window content, `POSTGRES_URL`, password, AWS access-key/secret/session-token material or any real customer data;
- the unsupported provider failure occurs before SES client construction/network send;
- the disposable DB identity is unchanged;
- the normal chatbot DB remains at migration 0017 and consultation tables remain absent;
- repo remains clean with no source/schema/migration change.

Gate D is a service/notification-boundary runtime smoke. It does not replace the HTTP/session/browser E2E required by Gate E.

If the notification unexpectedly succeeds, throws through `notifyConsultation`, changes the already-committed consultation state/event, leaks private content into captured logs, or any external network call is attempted, Gate D fails and execution stops.

Do not run Gate E/F in the same task.

### Step 6 — real app E2E on disposable DB

Start the chatbot with shell-scoped disposable `POSTGRES_URL`.

Use separate verified customer/admin/lawyer identities.

Exercise at minimum:

- customer new/history/detail;
- admin queue/detail + assignment + proposal;
- lawyer assigned queue/detail;
- customer confirmation;
- one reschedule cycle;
- one cancellation path;
- one completion path;
- a visible OCC/conflict refresh;
- notification-disabled normal behavior.

Do not run Legal Service paid/live calls as part of this appointment gate. Consultation UI acceptance does not require sending an AI query.

### Step 7 — bilingual/responsive visual acceptance

Capture/inspect:

- customer consultation history/new/detail;
- admin consultation queue/detail;
- lawyer consultation queue/detail;

for:

- desktop zh-CN;
- desktop English;
- mobile zh-CN;
- mobile English.

Verify no blocking overflow, long-email/note/instruction wrapping defects, hidden actions, misleading empty/unavailable states, or route confusion between lawyer review and consultation scheduling.

### Step 8 — evidence report

Report:

- source HEAD;
- disposable DB name only;
- normal DB journal before/after showing no change;
- disposable journal before/after;
- migration result;
- exact DB-backed test cases and pass/fail;
- concurrency outcomes;
- temporary rollback fault details and removal confirmation;
- notification fail-neutral result;
- E2E scenario results;
- visual acceptance results;
- all skips with reason;
- confirmation no real SES/AWS/OpenAI/S3/Stripe call;
- confirmation no commit/push/source change unless a separately reviewed defect correction was required.

### Stop rules

If migration, transaction semantics, authorization, concurrency or UI behavior fails:

STOP and preserve the disposable DB for inspection.

Do not manually mutate rows to make the test pass.

If the failure is a source defect, return to a bounded source patch and external review before resuming this gate.

Do not drop the disposable DB until external ChatGPT review and owner approval.

### P11-008 completion criterion

Only after this migrated-DB/runtime packet passes may P11-008 be marked ACCEPTED/VERIFIED and handed to P11-009.

This gate does not close P11-005 D-040 production-readiness items.

## Runtime Gate A+B acceptance — 2026-09-26

**Gate DB:** `chatbot_p11_008_gate_20260926_20c435`

Gate A+B are **ACCEPTED**.

Recorded evidence:

- normal source DB name: `chatbot`;
- PostgreSQL: 17.11;
- normal source journal before/after: `0017_wooden_silver_sable`;
- `ConsultationRequest` / `ConsultationEvent` absent on the normal DB before and after;
- disposable clone began at the same 0017 journal;
- repository `pnpm db:migrate` succeeded against the disposable DB only;
- disposable journal reached `0022_first_slayback`;
- both consultation tables exist on the disposable DB;
- all expected 0022 foreign keys and indexes were verified;
- no 0023 was generated;
- temporary dump was removed;
- repo remained clean;
- no external service call occurred.

The disposable clone remains retained for later runtime gates.

## Runtime Gate C acceptance — 2026-09-26

Gate C is **ACCEPTED** at current product-source checkpoint `62947e779b8a5a39df58038deab7c135e452569f`.

### Source correction discovered by Gate C

The first real proposal execution exposed a source defect in the overlap query: raw `sql\`...\`` comparison fragments passed JavaScript `Date` values without the timestamp-column encoder, causing postgres-js `ERR_INVALID_ARG_TYPE`.

Accepted correction `62947e7` replaced only those active-status/time predicates with typed Drizzle `inArray()/lt()/gt()` comparisons. Strict half-open semantics remain:

```text
existing.start < proposal.end
AND
existing.end > proposal.start
```

Bounded real-DB smoke after the correction proved ordinary proposal success, overlap -> domain 409, and adjacent boundary success.

### Harness calibration lesson

A later C6 false negative did not indicate a second source defect. Raw postgres-js decoded `timestamp without time zone` values as `Date` objects with an 11-hour Sydney-offset difference from the input epoch, while:

- the service return matched input;
- a fresh production Drizzle read matched input;
- timezone-neutral raw DB text matched the intended wall-clock timestamp;
- method/instructions/status/revision/event state were correct.

The canonical Gate-C harness was hardened to use production Drizzle reads where instant equality matters and to retain same-path comparisons for raw-driver neutrality checks.

Canonical harness SHA-256:

`e04f5655868d0e9cd450aa505a3ed9fcb15063fc28fa69020d6fa7585509d8d7`

### Final Gate-C result

Exactly **11 PASS / 0 FAIL / 0 SKIP**:

1. C1 create + immutable created event;
2. C2 verified-lawyer assignment;
3. C3 stale revision OCC neutral conflict;
4. C4 concurrent same-lawyer overlap serialization: one success / one 409 / no active overlap;
5. C5 exactly adjacent half-open slots both accepted;
6. C6 confirm -> re-propose with persisted fields, cleared confirmation and fresh-confirmation requirement;
7. C7 customer reschedule clears active proposal and returns to requested;
8. C8 cancel/complete state machine including terminal/invalid neutral rejection;
9. C9 assigned-lawyer read/action isolation;
10. C10 assignment vs lawyer-demotion race preserves the no-active-assignment-to-demoted-user invariant;
11. C11 disposable-only `P0001` event-insert fault proves request/event rollback atomicity, removes its temporary function/trigger and succeeds on clean retry.

The normal chatbot DB remained unchanged at migration 0017 with consultation tables absent. No external service call occurred. No source/schema/migration change was made during the final accepted run.

**Gate C is frozen. Do not rerun it merely for reassurance unless later product-source changes touch consultation transaction, scheduling, role-lock or event semantics.**

## Runtime Gate D execution contract — ACTIVE NEXT

Gate D uses the same retained disposable clone and the Step-5 contract above.

Purpose: prove that a real consultation mutation remains valid when the consultation email path fails locally **after commit**.

This gate must remain narrow:

- one fresh synthetic request;
- one valid staff cancellation;
- one `staff_cancelled` notification attempt;
- one deliberately unsupported email provider;
- zero real delivery/network;
- exact state/event/log safety evidence;
- no tracked source changes;
- stop for external review before Gate E.

Required temporary artifacts:

- `/tmp/p11_008_gate_d.ts` — canonical Gate-D harness;
- `/tmp/p11_008_gate_d_result.json` — machine-readable result;
- optionally a small runner that derives the retained disposable DB URL from the normal local URL without printing it.

The harness/result must not contain DB credentials or real customer data.

A successful Gate D result must report at least:

- expected branch/product checkpoint;
- verified disposable DB identity;
- synthetic fixture roles/count only;
- pre/post request status + revision;
- pre/post event counts;
- notification kind and boolean result;
- bounded log-safety assertions;
- explicit zero-external-call assertion;
- environment/console restoration;
- normal DB unchanged read-only check;
- clean repo / no source change / no commit-push.

Then STOP for external review. Gate E remains separate.
